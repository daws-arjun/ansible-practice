- name: Install packages
  hosts: local
  connection: local
  become: yes
  tasks:
    - name: Install services
      ansible.builtin.package:
        name: "{{ item }}"
        state: latest
      loop:
        - nginx
        - mysql
        - zip