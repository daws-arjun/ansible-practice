- name: install and start nginx
  host: frontend
  become: yes
  tasks:
    - name: install nginx
      ansible.builtin.package:
        name: nginx
        state: present

    - name: Start service nginx
      ansible.builtin.service:
        name: nginx
        state: started
        enabled: yes